export class CalendarOfflineCleaner {
    async cleanOfflineDb(offlineStorage, timeRangeDays, userId, now) {
        // nothing needs to specifically be done for Calendar right now,
        // but we need to have this to complement the mail one.
    }
}
//# sourceMappingURL=CalendarOfflineCleaner.js.map